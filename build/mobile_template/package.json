{
  "name": "openreality-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "OpenReality mobile WebView shell",
  "scripts": {
    "build:android": "npx cap add android && npx cap sync android && npx cap open android",
    "build:ios": "npx cap add ios && npx cap sync ios && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/cli": "^6.0.0"
  }
}
